# Implement the uploaded Bot Town update

## Scope
- Replace the changed Bot Town interface and simulation files with the uploaded versions.
- Add the uploaded live-town helper, public agent description, and backend design notes.
- Preserve the existing TanStack setup and unchanged 3D scene files.
- Keep the app self-contained: no database, login, payment, or external service setup.

## Verification
- Confirm the project compiles through the live preview.
- Check the town on desktop and mobile.
- Test resident selection, activity updates, controls, and dialogs.
- Confirm there are no blocking browser errors or layout overlaps.

## Technical details
The archive matches the current React, TypeScript, TanStack Start, React Three Fiber, and Zustand stack. Only the files that differ from the current project will be imported; Git metadata, dependencies, and generated output remain excluded.
